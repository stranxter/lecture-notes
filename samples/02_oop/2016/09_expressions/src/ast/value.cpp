#include <iostream>
#include <assert.h>
#include <math.h>

#include "value.h"


Value* Value::plus (Value *other) 
{
	assert (false);
	return NULL;
};
Value* Value::power (Value *other) 
{
	assert (false);
	return NULL;
};

bool Value::toNativeBool ()
{
	assert (false);
	return false;
}

